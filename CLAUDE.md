# CS & 면접 학습 아카이브 — 프로젝트 지침

이 저장소는 AI와 함께 공부한 내용을 **자동으로 구조화 기록**하기 위한 공간이다.
여기서 진행하는 학습 활동은 두 가지뿐이다: **자료정리**와 **모의면접**.
(일상적인 개념 Q&A는 다른 도구에서 하므로 이 프로젝트의 대상이 아니다.)

---

## 저장 위치

Notion에 **두 개의 DB**가 있고, 둘 다 허브 페이지 "CS & 면접 학습 아카이브" 아래에 있다.

### Notion DB ① CS 학습 로그 (세션 기록 / 시간순)
- **Data source ID: `8f362694-fac3-46a7-9e89-f39f77693caf`**
- DB URL: https://app.notion.com/p/bcb3b2fc7184470cbe3fdfbb3c5268de
- **한 세션 = 한 행.** "언제 무엇을 했고 약점이 무엇이었나"의 기록.

### Notion DB ② 개념 노트 (복습용 / 개념별 누적)
- **Data source ID: `643b30c2-48b4-454e-bbc6-05df11ca873a`**
- DB URL: https://app.notion.com/p/f58e8d2b344a4c7d925c7c7d9a57e56d
- **개념당 1행**, 재학습할 때마다 **갱신·누적**. `분야`로 그룹핑하면 과목 안에 개념들이 묶여 보인다.
- 학습 로그와 `관련 세션`(Relation)으로 연결 — 이 개념이 나온 세션을 추적.
- **나중에 다시 보는 정리 노트가 여기에 쌓인다.**

> ⚠️ 두 Data source ID는 워크스페이스마다 다르다. 새 계정/PC 재현 시 [SETUP.md](SETUP.md) 참고해 갱신.
> 기록은 Notion 커넥터(`notion-create-pages` / `notion-update-page`)로 추가하며, 부모(parent)를 해당 `data_source_id`로 지정한다.

### 로컬 백업 — 마크다운
- 자료정리 → `Notes/`
- 모의면접 → `Interviews/`
- 파일명 규칙: `YYYY-MM-DD_분야_제목.md`

> 모든 기록은 **Notion + 로컬 .md 둘 다**에 남긴다. (한쪽이 날아가도 복구 가능하게)

---

## DB 속성

### ① CS 학습 로그
| 속성 | 값 |
|---|---|
| 제목 | `YYYY-MM-DD 분야 주제` |
| 날짜 | 세션 날짜 (date:날짜:start) |
| 유형 | `자료정리` 또는 `모의면접` |
| 분야 | OS / 네트워크 / DB / 자료구조·알고리즘 / 컴퓨터구조 / AI·ML / 데이터 엔지니어링 / 기타 |
| 이해도 | 🔴 부족 / 🟡 보통 / 🟢 충분 |
| 약점·복습포인트 | 다음에 복습할 핵심 (모의면접에서 특히 중요) |
| 블로그 후보 | 잘 정리돼 공개할 만하면 체크 |
| (본문) | 정리본 또는 면접 리포트 전문 |

### ② 개념 노트
| 속성 | 값 |
|---|---|
| 제목 | 개념명 (예: `해시 테이블`) |
| 분야 | (위와 동일 8개) — 그룹핑 기준 |
| 이해도 | 🔴 / 🟡 / 🟢 |
| 핵심요약 | 한눈 요약 |
| 복습포인트 | 자주 틀리는/헷갈리는 지점 |
| 관련 세션 | 이 개념이 나온 학습 로그 행 연결(Relation) |
| 블로그 후보 | 발행 후보면 체크 |
| (본문) | **정리 노트 전문** |

> **개념 노트 작성 규칙 (자료정리·모의면접 공통, 반드시 준수).**
> 1. 개념 노트를 쓰기 전 **항상 먼저 `개념 노트` DB에서 같은 개념(제목+분야)이 있는지 검색**한다 (`notion-search` 또는 `notion-query-data-sources`).
> 2. **이미 있으면 새 행을 만들지 말고** 기존 행을 `notion-update-page`로 **수정·보충**한다 — 기존 내용을 통째로 덮어 지우지 말고, 틀린 부분만 바로잡고 빠진 내용을 더한다. (이해도·복습포인트도 최신 상태로 갱신)
> 3. **없을 때만** 새 행을 생성한다.
> → 결과적으로 **개념당 1행**이 유지되며, 학습을 반복할수록 그 노트가 누적·발전한다.

---

## 모드 1: 자료정리
사용자가 강의 자료(파일)를 주거나 주제를 정하면:
1. 내용을 구조화해 설명·정리한다 (핵심 개념 → 흐름 → 예시).
2. 사용자가 이해했는지 중간중간 확인하며 진행한다.
3. 기록 시:
   - **개념 노트 ②** 에 개념별 정리본을 본문으로 기록(기존 개념이면 갱신).
   - **학습 로그 ①** 에 세션 행을 남기고 헷갈렸던 지점을 `약점·복습포인트`에.

## 모드 2: 모의면접
**면접을 시작하기 전 반드시 [Interviews/_PROTOCOL.md](Interviews/_PROTOCOL.md)를 읽고 그 규칙을 따른다.** 요약:
1. 실제 면접처럼 **한 번에 한 질문씩**, 꼬리 질문으로 깊이를 압박 검증한다. (개념 질문 중엔 정답·해설 금지)
2. **개념(섹션)이 끝날 때마다 「개념 마무리 체크포인트」**: 피드백·모범답안 공개 → 사용자 질문받기 → **기록** → 확인 후 다음. (임의로 다음 주제 직행 금지)
3. 체크포인트 기록 시:
   - **개념 노트 ②** 에 그 개념 정리(핵심·모범답안 요점·자주 틀리는 점)를 누적(기존 개념이면 갱신).
   - **학습 로그 ①**(유형=모의면접)에 세션 리포트를 누적하고 `약점·복습포인트` 채움.
4. **막힌 질문 = 약점.**

> 음성(녹음 파일) 답변 분석은 현재 범위 밖(2차 추가 예정). 지금은 텍스트 면접만.

---

## 자동 기록 규칙
- 한 주제/세션이 끝나거나 대화가 길어지면, **사용자가 시키지 않아도 먼저 기록을 제안하고 진행**한다.
- 긴 세션은 앞부분이 컨텍스트에서 밀리기 전에 **중간 체크포인트**로 미리 기록한다.

## 트리거
- **"시작"** → Notion "CS 학습 로그"에서 최근 기록과 `약점·복습포인트`가 있는 항목(및 개념 노트의 🔴/🟡 항목)을 조회해, "지난 진도 + 복습할 약점"을 3~5줄로 브리핑한 뒤 학습 시작.
- **"정리" / "기록" / "면접 종료"** → 즉시 현재 세션을 Notion(학습 로그 + 개념 노트) + 로컬에 기록.
