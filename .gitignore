# 강의 원본 자료 (저작권/용량) — 정리본과 Notion만 공유
/Materials/

# 로컬 전용 Claude 설정 (권한 잠금 settings.json은 공유)
.claude/settings.local.json

# OS / 에디터
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# 환경 / 비밀
.env
.env.*
*.secret

# MCP 서버 설정 — 토큰이 들어갈 수 있으므로 절대 커밋 금지.
# 커넥터는 반드시 local 스코프로 추가할 것 (`claude mcp add --scope local ...`).
# project 스코프는 이 파일을 만들어 공유 저장소에 올린다.
.mcp.json
